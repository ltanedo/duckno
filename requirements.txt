# Example of installing the released package directly from the GitHub tag
# Replace v0.1.0 with the tag created from piphub.yaml (prefix + version)
# Either install directly:
#   pip install git+https://github.com/ltanedo/duckno.git@v0.1.0#egg=duckno
# Or via this requirements file:
git+https://github.com/ltanedo/duckno.git@v0.1.0#egg=duckno
